<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.infnet.tictacapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tictac"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_tictac_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicTacApp"
        android:versionCode="1"
        android:versionName="TicTacToe-1.0"
        >

        <activity
            android:name=".RankingActivity"
            android:exported="false" />

        <activity
            android:name=".FirebaseLogin"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2601036311274042~1829619665" />

        <activity
            android:name=".splash_screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TelaInicio"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>